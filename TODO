TODO:

Server:

OK* finish implementation of the generic RESTful API for the simple case (PATCH, DELETE...)
  * handle nested resources
  * handle app authentication
  * separate user-private VS world-public data
  * handle user-specific authentication

Cient:

  * make a very simple app and find a way to add/extract metadata from app's model for "automatic" backend handling
